test_kind: resmoke_selftest_timeout

selector:
  roots:
  - jstests/resmoke_selftest/e2e/timeout/noPassthrough/*.js

# js tests will start their own mongods
executor:
  config:
    shell_options:
      nodb: ''
      readMode: commands
