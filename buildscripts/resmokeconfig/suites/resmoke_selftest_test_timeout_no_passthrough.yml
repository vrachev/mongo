test_kind: resmoke_selftest_timeout

selector:
  roots:
  - jstests/resmoke_selftest/end2end/timeout/noPassthrough/timeout2.js

# js tests will start their own mongods
executor:
  config:
    shell_options:
      nodb: ''
      readMode: commands
